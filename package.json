{
  "name": "handled",
  "version": "1.0.0",
  "description": "Handled 3PL Platform - Internal tools and data management",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "dev:api": "pnpm --filter @handled/api run dev",
    "dev:web": "pnpm --filter @handled/web run dev",
    "build": "pnpm -r run build",
    "lint": "pnpm -r run lint",
    "db:migrate": "bash database/migrate-all.sh",
    "db:migrate:primary": "bash database/migrate-primary.sh",
    "db:migrate:data": "bash database/migrate-data.sh",
    "db:migrate:status": "psql -U handled_user -d handled -c 'SELECT version, description, applied_at, execution_time_ms FROM config.schema_migrations ORDER BY version;'",
    "db:migrate:status:primary": "psql \"$PRIMARY_DATABASE_URL\" -c 'SELECT version, description, applied_at, execution_time_ms FROM config.schema_migrations ORDER BY version;'",
    "db:migrate:status:data": "psql \"$DATA_DATABASE_URL\" -c 'SELECT version, description, applied_at, execution_time_ms FROM config.schema_migrations ORDER BY version;'"
  },
  "keywords": ["3pl", "logistics", "warehouse"],
  "author": "",
  "license": "UNLICENSED"
}

